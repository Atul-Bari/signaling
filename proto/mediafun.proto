syntax = "proto3";

package say;

service Makecall {
    rpc sdpexchange(sdp) returns(sdp) {}
}

message sdp {
    string fromid=1;
    string toid=2;
    string offer = 3;
    string sessionid=4;

}
